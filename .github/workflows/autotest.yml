name: Autotests
on: push
jobs:
  lint:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run:  npm ci
      - name: Lint
        run: npn run lint
